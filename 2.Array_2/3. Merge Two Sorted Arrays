#include <bits/stdc++.h>
vector<int> ninjaAndSortedArrays(vector<int>& a1, vector<int>& a2, int m, int n) 
{
	int k=m;
	for(int i=0;i<n;i++)
	{
		a1[k] = a2[i];
		k++;
	}
	sort(a1.begin(),a1.end());
	return a1;
}

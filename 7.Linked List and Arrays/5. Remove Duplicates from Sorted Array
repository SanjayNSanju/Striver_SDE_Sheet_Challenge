int removeDuplicates(vector<int> &arr, int n) 
{
	// Write your code here.
	int prev = arr[0];
	int count=1;
	for(int i=1;i<n;i++)
	{
		if(arr[i]!=prev)
		{
			prev = arr[i];
			count++;
		}
	}
	return count;
}
